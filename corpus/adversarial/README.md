# Adversarial scaling families

- `adversarial_scaling.jl` — fan-in-k (factorisation stays linear) and mesh-w
  (cost grows with treewidth) family generators + the scaling sweep
- `adversarial.csv`, `adversarial_factored.csv` — the recorded operation counts / timings
  behind the Probability chapter's `tab:pp-adversarial` and `fig07_crossover`

Generated from recorded seeds inside `adversarial_scaling.jl`; see also
`../random-and-mutated/graph_families.jl`.
